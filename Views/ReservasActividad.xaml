<Window x:Class="Views.ReservasActividad"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hoja de Reservas por Actividad" Height="160" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#FFFAFAFA">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Hoja de Reservas por Actividad" FontSize="24" FontWeight="Bold" Foreground="#2E8B57" Margin="0,0,0,20" Grid.Row="0" HorizontalAlignment="Center"/>
        
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,20" HorizontalAlignment="Center">
            <TextBlock Text="Actividad:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0"/>
            <ComboBox x:Name="comboActividades" Width="200" Height="30" Margin="0,0,10,0"/>
            <Button x:Name="btnGenerar" Content="Generar Informe" Width="130" Height="30" Background="#2E8B57" Foreground="White" FontWeight="Bold" Click="btnGenerar_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock x:Name="txtOcupacion" FontWeight="Bold" Foreground="#2E8B57"/>
        </StackPanel>
    </Grid>
</Window>
